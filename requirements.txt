torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
rembg[gpu]>=2.0.50
onnxruntime-gpu>=1.16.0
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
safetensors>=0.4.0
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0
tqdm>=4.66.0
huggingface_hub>=0.20.0
matplotlib>=3.7.0
gradio>=4.44.0
# Modo AO VIVO (webcam -> camera virtual; roda em CPU)
mediapipe>=0.10.35
pyvirtualcam>=0.15.0
pygrabber>=0.2          # lista nomes das cameras (Windows/DirectShow) no app de camera
# Motor de matting de alta qualidade (RVM) no modo live — opcional, mais pesado
# torch/torchvision ja listados acima; o modelo rvm_mobilenetv3.pth (~15MB) baixa via torch.hub
